# src/test/resources/bootstrap-test.properties
# ¡ARCHIVO CRÍTICO! - Deshabilita el bootstrap context para tests
spring.cloud.bootstrap.enabled=false
spring.cloud.vault.enabled=false
spring.cloud.vault.config.enabled=false
spring.cloud.config.enabled=false

# Configuraciones adicionales para evitar problemas
management.health.vault.enabled=false

# Logging para debug si es necesario
logging.level.org.springframework.cloud=ERROR
logging.level.org.springframework.vault=ERROR